# CLAUDE.md — рабочая память между сессиями

> Этот файл читается в начале **каждой** сессии Claude Code (он часть клона репозитория).
> Он служит «памятью предыдущей сессии»: правила работы + бриф активного проекта + журнал.
> **Поддерживай этот файл в актуальном состоянии**: в конце работы добавляй запись в «Журнал сессий»
> и обновляй раздел «Активный проект».

## ⚠️ START HERE — прочитай первым делом
- **Это окружение пользователь использует как рабочее пространство для РЕКЛАМНОГО ВИДЕО, а не для разработки софта.**
- Когда пользователь говорит «ролик», «реклама», «маскот», «персонаж», «SVOY VPN», «озвучка», «логотип» — это
  про **видео-проект** (см. «Активный проект» ниже). **НЕ нужно писать/разрабатывать VPN-продукт или код.**
- Инструменты для работы: **MCP Higgsfield** (генерация изображений/видео/аудио) и `ffmpeg`. Не трогай Java/DNS
  код этого репозитория, если об этом прямо не попросили.
- Общение — **на русском**. Действуешь сам (см. «Главное правило работы»).
- Если пользователь продолжает проект — сориентируйся по разделу «Активный проект» и «Журнал сессий», не
  переспрашивая базовое.

## 🎯 ТЕКУЩАЯ ЗАДАЧА — выполни ровно это, НИЧЕГО не выдумывай
**Финал ролика = взять УЖЕ ГОТОВОЕ немое видео маскота и наложить на него русскую озвучку.**
❌ НЕ генерировать новый «бренд-ревил/CTA/стинги», НЕ придумывать новый сюжет, НЕ делать новое видео.
✅ Это задача «скачать + TTS + склейка», видео генерировать НЕ нужно.

Готовое видео (немое, 1080p, 9:16, 15с):
`https://d8j0ntlcm91z4.cloudfront.net/user_3Ek1JHHvfr8D4qa9ou43dNuTnrN/hf_20260613_211210_7cdb6bf5-a754-49e2-a15e-d17fef588500.mp4`

Шаги (нужна Full-сеть; всё делаешь сам, результат — вложением через SendUserFile):
1. Если нет ffmpeg: `apt-get update && apt-get install -y ffmpeg`.
2. Скачать видео: `curl -L -o video.mp4 "<URL выше>"`.
3. Русская озвучка по скрипту из «Активный проект»:
   - предпочтительно **ElevenLabs** (модель `eleven_multilingual_v2`, русский мужской deep/calm), ключ в env `ELEVENLABS_API_KEY`.
     Голос выбрать через `GET https://api.elevenlabs.io/v1/voices`; синтез — `POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}` → `voice.mp3`.
   - запасной вариант — Higgsfield `inworld_text_to_speech`, голос `Dmitry (ru)`.
4. Склейка: `ffmpeg -i video.mp4 -i voice.mp3 -map 0:v:0 -map 1:a:0 -shortest -c:v copy final.mp4`.
5. Отдать `final.mp4` пользователю вложением (**SendUserFile**).

> ⚠️ Инструменты Higgsfield (`generate_image`/`generate_video`/`generate_audio`/`job_display` и т.д.) —
> **отложенные (deferred)**: их нужно подгрузить через ToolSearch ПЕРЕД использованием.
> НЕ заявляй «видео/картинки недоступны», пока не поискал тулзу. Но для текущей задачи они и не требуются.

## Язык
Общаться с пользователем **на русском**.

## Главное правило работы (durable)
- **Всё, что технически возможно, делаю я сам** — через API-ключи, MCP, CLI, ffmpeg, любым способом.
  Пользователь **не выполняет ручных шагов**.
- Схема: я предлагаю подход → пользователь подтверждает («да, давай через X») → **дальше всё исполняю я сам, до готового результата** (готовый файл отдаю вложением через SendUserFile).
- Не перекладывать на пользователя склейку/загрузку/генерацию, если это можно сделать самому.
- Не читать пользователю лекции по безопасности без запроса — он сам решает свой риск-профиль.

## Окружение
- Network access: **Full** (выставлено пользователем). Если внешний хост недоступен — скорее всего сессия
  стартовала со старой политикой; нужна **новая сессия** (egress фиксируется при старте контейнера).
- `ffmpeg` ставится setup-скриптом окружения (`apt update && apt install -y ffmpeg`).
- **Секреты — только через переменные окружения**, не в гит. Ключ ElevenLabs: переменная `ELEVENLABS_API_KEY`.
- **Доставка роликов в Telegram (durable, по просьбе пользователя 2026-06-16):** каждый готовый ролик
  дублировать пользователю в Telegram **ВСЕГДА КАК ФАЙЛ/ДОКУМЕНТ** (`sendDocument`, без сжатия — НЕ `sendVideo`).
  Отдельного Telegram-MCP нет → слать через **Telegram Bot API** (`curl`
  `https://api.telegram.org/bot<TOKEN>/sendDocument` с `chat_id` и `document=@file`). Нужны переменные окружения
  `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` (chat_id можно получить через `getUpdates` после сообщения боту).
  Токен в гит НЕ коммитить (в этой сессии лежит в `/tmp/tg.env`, бот @crypotecbot, chat_id 5406143589).
  Также всегда отдавать ролик файлом через SendUserFile. Лимит Bot API на файл — 50 МБ; если больше — сжать.
- Доступные MCP: **Higgsfield** (генерация изображений/видео/аудио, ходит через серверы Anthropic, работает
  независимо от allowlist) и **GitHub**.
- Higgsfield user-префикс CDN: `user_3Ek1JHHvfr8D4qa9ou43dNuTnrN`.

---

## Активный проект: рекламный ролик «SVOY VPN» (маскот-блогер)

**Идея:** 2D-персонаж-супергерой как блогер-обзорщик рекламирует VPN-сервис «SVOY VPN», держа в руках
круглый логотип и рассказывая о плюсах. Формат **9:16**, один клип ≤ **15 сек**.

**Канон персонажа (не менять):**
- Плоский **2D cel-shaded** мультяшный супергерой; **зелёная маска** на глаза; зелёно-чёрный костюм.
- На груди — вордмарк **«SVOY VPN» латиницей**.
- Ярче + лёгкое свечение. Вживлён в **реальную фотографичную комнату** (спальня блогера) с тенью и светом
  («2D-тоон попал в реальный мир»). НЕ делать реалистичным 3D.

**Озвучка:** строго **русский**, серьёзный/глубокий мужской голос «которому доверяешь».
Голоса Inworld TTS (Dmitry, Nikolai) пользователю **не понравились** (шепелявый/неестественный).
План: генерировать голос в **ElevenLabs** (модель Eleven Multilingual v2, русский мужской deep/calm)
через `ELEVENLABS_API_KEY`.

**Финальный скрипт (рус, ~15 сек):**
> Привет! Смотри — Свой ВПН. Скорость космос: видео и сайты летают без тормозов. Цена смешная,
> есть бесплатный пробный период, а подключение — одна кнопка. Свой ВПН — ставь и пользуйся!

**Готовые ассеты (Higgsfield CDN, база `https://d8j0ntlcm91z4.cloudfront.net/user_3Ek1JHHvfr8D4qa9ou43dNuTnrN/`):**
- Финальное видео 1080p, 9:16, 15с, **без звука** (Kling 3.0 → Topaz upscale):
  `hf_20260613_211210_7cdb6bf5-a754-49e2-a15e-d17fef588500.mp4`  (job `7cdb6bf5`)
- Кадр-основа (персонаж с логотипом в руках): job `7ec7fa18`
- Вырезанный круглый логотип (без фона): job `f1693aad`
- Черновая русская дорожка Dmitry (inworld): job `1b77d868`

**Что осталось (делаю сам при открытой сети):**
1. Сгенерировать голос в ElevenLabs по скрипту (русский мужской deep/calm), скачать mp3.
2. Скачать финальное видео 1080p с CDN.
3. Склеить: `ffmpeg -i video.mp4 -i voice.mp3 -map 0:v:0 -map 1:a:0 -shortest -c:v copy final.mp4`.
4. Отдать `final.mp4` пользователю вложением (SendUserFile).

---

## Журнал сессий
- **2026-06-13:** Собрал персонажа (2D, маска, «SVOY VPN» на груди), вживил в реальную спальню (тень/свет),
  вырезал круглый логотип и дал в руки, сделал немой ролик 15с (Kling) и апскейл до 1080p. Сгенерил черновую
  русскую TTS-дорожку (Dmitry). Завис на финале: ElevenLabs и склейка требуют сетевого доступа, а сессия
  стартовала со старой (закрытой) политикой. Следующий шаг — в сессии с Full-доступом: голос ElevenLabs +
  ffmpeg-склейка → готовый ролик со звуком.
- **2026-06-16:** Новый формат — рекламный **ASMR-Unboxing** (по референсу Higgsfield «FROGGY PRINCE»),
  полностью под бренд SVOY VPN. Вид сверху, руки **маскота** (зелёные перчатки костюма с неоновыми линиями,
  «тоон в реальном мире»). Распаковываемая вещь по выбору пользователя — **круглый логотип SVOY VPN как
  глянцевый коллекционный медальон** + карточки (логотип + супергерой). Звук: ASMR-фоли + лёгкая музыка.
  Пайплайн: опорные кадры (nano_banana_2, 2K, референсы — бейдж `f1693aad` + маскот `2ec1fee9`) → Kling 3.0 pro
  (start/end, 1080×1920, sound on). Сделал 10-сек версию, затем по правке — длиннее: т.к. у всех видео-моделей
  максимум 15с, склеил два 15-сек шота через общий кадр (последний кадр клипа 1 = старт клипа 2) → бесшовные
  **~30 сек** с секцией «обзора» (крутит медальон, поворачивает карточки). Файлы в `unboxing/`:
  `svoy_unboxing_10s.mp4`, `clip1.mp4`, `clip2.mp4`, `svoy_unboxing_30s.mp4`. Ветка `claude/beautiful-euler-zuxkxs`,
  черновой PR #13. Баланс Higgsfield Ultra (~265 кредитов после работы).
- **2026-06-16 (туториал):** По формату Higgsfield «Tutorial/Product Review» — рекламный туториал SVOY VPN.
  Ведущий = 2D-маскот-комментатор на бровке стадиона ЧМ (гарнитура с микрофоном, трибуны ~80k, игроки сзади,
  фон чуть размыт). Продукт = **круглое устройство в форме логотипа**: показывает логотип → переворачивает →
  круглый сенсорный экран → действия на экране: Telegram «Старт» → «3 дня бесплатно» → «Купить подписку 249 ₽»
  → приложение с серверами и «ПОДКЛЮЧЕНО». Озвучка — **ElevenLabs** (голос AlexandrRU `Ubxxp0M2iEu47zdO9O3J`,
  модель eleven_multilingual_v2) + гул трибун (Higgsfield `mirelo_text_to_audio`). v1 был с прямоугольным телефоном
  и плоской вставкой-скринкастом (`svoy_tutorial.mp4`); по правке пользователя переделал на круглое устройство —
  3 шота Kling 3.0 pro по 11с (start/end по опорным кадрам) склеены ffmpeg → ~33с (`svoy_tutorial_round.mp4`).
  Доставка: SendUserFile + Telegram sendDocument. Озвучку лучше делать ПОСЛЕ утверждения визуала.
