# CLAUDE.md — рабочая память между сессиями

> Этот файл читается в начале **каждой** сессии Claude Code (он часть клона репозитория).
> Он служит «памятью предыдущей сессии»: правила работы + бриф активного проекта + журнал.
> **Поддерживай этот файл в актуальном состоянии**: в конце работы добавляй запись в «Журнал сессий»
> и обновляй раздел «Активный проект».

## Язык
Общаться с пользователем **на русском**.

## Главное правило работы (durable)
- **Всё, что технически возможно, делаю я сам** — через API-ключи, MCP, CLI, ffmpeg, любым способом.
  Пользователь **не выполняет ручных шагов**.
- Схема: я предлагаю подход → пользователь подтверждает («да, давай через X») → **дальше всё исполняю я сам, до готового результата** (готовый файл отдаю вложением через SendUserFile).
- Не перекладывать на пользователя склейку/загрузку/генерацию, если это можно сделать самому.
- Не читать пользователю лекции по безопасности без запроса — он сам решает свой риск-профиль.

## Окружение
- Network access: **Full** (выставлено пользователем). Если внешний хост недоступен — скорее всего сессия
  стартовала со старой политикой; нужна **новая сессия** (egress фиксируется при старте контейнера).
- `ffmpeg` ставится setup-скриптом окружения (`apt update && apt install -y ffmpeg`).
- **Секреты — только через переменные окружения**, не в гит. Ключ ElevenLabs: переменная `ELEVENLABS_API_KEY`.
- Доступные MCP: **Higgsfield** (генерация изображений/видео/аудио, ходит через серверы Anthropic, работает
  независимо от allowlist) и **GitHub**.
- Higgsfield user-префикс CDN: `user_3Ek1JHHvfr8D4qa9ou43dNuTnrN`.

---

## Активный проект: рекламный ролик «SVOY VPN» (маскот-блогер)

**Идея:** 2D-персонаж-супергерой как блогер-обзорщик рекламирует VPN-сервис «SVOY VPN», держа в руках
круглый логотип и рассказывая о плюсах. Формат **9:16**, один клип ≤ **15 сек**.

**Канон персонажа (не менять):**
- Плоский **2D cel-shaded** мультяшный супергерой; **зелёная маска** на глаза; зелёно-чёрный костюм.
- На груди — вордмарк **«SVOY VPN» латиницей**.
- Ярче + лёгкое свечение. Вживлён в **реальную фотографичную комнату** (спальня блогера) с тенью и светом
  («2D-тоон попал в реальный мир»). НЕ делать реалистичным 3D.

**Озвучка:** строго **русский**, серьёзный/глубокий мужской голос «которому доверяешь».
Голоса Inworld TTS (Dmitry, Nikolai) пользователю **не понравились** (шепелявый/неестественный).
План: генерировать голос в **ElevenLabs** (модель Eleven Multilingual v2, русский мужской deep/calm)
через `ELEVENLABS_API_KEY`.

**Финальный скрипт (рус, ~15 сек):**
> Привет! Смотри — Свой ВПН. Скорость космос: видео и сайты летают без тормозов. Цена смешная,
> есть бесплатный пробный период, а подключение — одна кнопка. Свой ВПН — ставь и пользуйся!

**Готовые ассеты (Higgsfield CDN, база `https://d8j0ntlcm91z4.cloudfront.net/user_3Ek1JHHvfr8D4qa9ou43dNuTnrN/`):**
- Финальное видео 1080p, 9:16, 15с, **без звука** (Kling 3.0 → Topaz upscale):
  `hf_20260613_211210_7cdb6bf5-a754-49e2-a15e-d17fef588500.mp4`  (job `7cdb6bf5`)
- Кадр-основа (персонаж с логотипом в руках): job `7ec7fa18`
- Вырезанный круглый логотип (без фона): job `f1693aad`
- Черновая русская дорожка Dmitry (inworld): job `1b77d868`

**Что осталось (делаю сам при открытой сети):**
1. Сгенерировать голос в ElevenLabs по скрипту (русский мужской deep/calm), скачать mp3.
2. Скачать финальное видео 1080p с CDN.
3. Склеить: `ffmpeg -i video.mp4 -i voice.mp3 -map 0:v:0 -map 1:a:0 -shortest -c:v copy final.mp4`.
4. Отдать `final.mp4` пользователю вложением (SendUserFile).

---

## Журнал сессий
- **2026-06-13:** Собрал персонажа (2D, маска, «SVOY VPN» на груди), вживил в реальную спальню (тень/свет),
  вырезал круглый логотип и дал в руки, сделал немой ролик 15с (Kling) и апскейл до 1080p. Сгенерил черновую
  русскую TTS-дорожку (Dmitry). Завис на финале: ElevenLabs и склейка требуют сетевого доступа, а сессия
  стартовала со старой (закрытой) политикой. Следующий шаг — в сессии с Full-доступом: голос ElevenLabs +
  ffmpeg-склейка → готовый ролик со звуком.
