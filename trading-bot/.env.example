# Bybit API credentials — used ONLY in live mode (incl. Demo Trading).
# Create demo keys at: Bybit -> Demo Trading -> API.
# NEVER commit the real .env file.
BYBIT_API_KEY=your_key_here
BYBIT_API_SECRET=your_secret_here
